import scriptGroupBlock from "@/services/modules/scriptGroupBlock.services";

const state = {
  groups: {}
};

const getters = {
  groups: state => state.groups
};

const mutations = {};

const actions = {};
export default {
  state,
  getters,
  mutations,
  actions
};